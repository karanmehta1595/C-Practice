#include <stdio.h>

int main() {
                float a,b,c,d;
                a=10.1;
                b=10.5;
                c=12.8;
                d=1.47;

                float z = (a+b)/(c*d);

        printf("Z = %.2f\n", z);

    return 0;
}